import javax.swing.JButton;
import javax.swing.JFrame;
import javax.swing.JLabel;
import javax.swing.JPanel;

public class FirstGUITester {
	
	public static void main(String[]args){
		
		
		//JFrame frame = new JFrame();	
		
		//JButton button = new JButton("Click me!");
		//JLabel label = new JLabel("Hello World");
		
		
		 /* If you have more than one component, put them into a panel (a container for other
		/ * user-interface components), and then add the panel to the frame:
		 
		JPanel panel = new JPanel();
		panel.add(button);
		panel.add(label);
		frame.add(panel);
		
		final int FRAME_WIDTH = 300;
		final int FRAME_LENGTH = 400;
		
		frame.setSize(FRAME_WIDTH,FRAME_LENGTH);
		frame.setTitle("Test Frame");
		frame.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);
		frame.setVisible(true);**/
		
		MyFrame testFrame2 = new MyFrame();
	}
}
